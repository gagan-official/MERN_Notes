# Clusters (Built-In Module)

- Can create Multiple Instances of your single nodejs server each having their separate v8 Engine and JS Runtime Environment.
- Best used for handling load balancing but manually.

If we have current running nodejs server, and we want to scale it to handle multiple which forks 